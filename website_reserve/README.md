# Reserve Website

This folder is intentionally reserved as a fallback/backup website.

- The live GitHub Pages website is in `../website/`.
- If we ever replace the live site, copy the previous one here before switching.
