
  # AITT Website

  Official website for AI Trading Terminal (AITT) - An offline-first AI-assisted trading system.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  